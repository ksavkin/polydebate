[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd backend && python3 -m pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120 --worker-class gevent"

